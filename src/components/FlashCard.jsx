import React from "react";
import { Paper } from "@mui/material";

// check agaon what it means
const FlashCard = ({ children }) => {
    
    return (
    <Paper >
        {children}
    </Paper>
    )
};

export default FlashCard;
